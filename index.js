const divide = function (num1, num2) {
    if (num1 !== undefined && num2 !== undefined) {
      return num1 / num2;
    } else {
      return 2000 / 100;
    }
}
const square= (x) => x * x;
square (2);

const add = (a, b ) => a+ b;
add (2 ,3)